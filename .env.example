# Copy to .env and fill in. NEVER commit your real .env.
#
# The __client cookie: DevTools > Application > Cookies > https://higgsfield.ai
HIGGSFIELD_CLERK_COOKIE=

# Session id: DevTools > Console > window.Clerk.session.id  (starts with sess_)
HIGGSFIELD_SESSION_ID=sess_xxxxxxxxxxxxx

# Extra cookies forwarded to the API. The /jobs create endpoint is behind DataDome;
# without your browser's datadome cookie, POST /jobs/{model} returns a 403 captcha.
# Get it from DevTools > Console > document.cookie (copy at least the datadome=... part).
# This cookie rotates — refresh it if generation starts returning 403.
HIGGSFIELD_EXTRA_COOKIES=datadome=xxxxxxxx

# --- Extra accounts (parallel) ---------------------------------------------
# One account serialises behind a per-account rate limit (429 rate_limit_reached).
# Add more accounts to run jobs in parallel; the pool load-balances and fails a
# job over to another account on 429. Numbered _2, _3, _4, _5 ... (each with its
# own session id, __client cookie, and datadome cookie).
# HIGGSFIELD_SESSION_ID_2=sess_yyyy
# HIGGSFIELD_CLERK_COOKIE_2=eyJ...
# HIGGSFIELD_EXTRA_COOKIES_2=datadome=yyyy
# HIGGSFIELD_SESSION_ID_3=sess_zzzz
# HIGGSFIELD_CLERK_COOKIE_3=eyJ...
# HIGGSFIELD_EXTRA_COOKIES_3=datadome=zzzz

# Per-account concurrent tier: 4 / 8 / 12 / 16 (total parallelism = this x accounts)
HIGGSFIELD_MAX_CONCURRENT=4

# Minimum seconds between API requests PER ACCOUNT (+ up to 50% random jitter), so
# traffic looks human and doesn't trip DataDome. Raise it if you still see 403s;
# set 0 to disable. 1.5-3 is a safe range.
HIGGSFIELD_MIN_REQUEST_INTERVAL=1.5

# Default image model when not specified
HIGGSFIELD_DEFAULT_MODEL=nano-banana-2

# One of: 1k / 2k / 4k
HIGGSFIELD_DEFAULT_RESOLUTION=2k

# Where downloads land
HIGGSFIELD_OUTPUT_DIR=./higgsfield_output

# DEBUG / INFO / WARNING / ERROR
HIGGSFIELD_LOG_LEVEL=INFO
